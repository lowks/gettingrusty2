pub fn nth(n: u32) -> u32 {
    let mut count = 0;
    match prime_true(n) {
        
    }
    unimplemented!("What is the 0-indexed {}th prime number?", n)
}

fn prime_true(n: u32) -> bool {
    match n {
        
    }
}